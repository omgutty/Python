- Wants generated/working folders (e.g., the `autoweave` folder) added to `.gitignore` so they are not pushed to GitHub — keeps the repo clean of auto-generated/scratch content. Confidence: 0.6
- Prefers each topic/concept to get its own separate study-reference file in the Task folder rather than mixing new content into an existing practice file — explicitly asked to "create separate file in Task folder and add all above content in that file" (e.g., the brackets explanation became its own `brackets.py` instead of being appended to `IQ.py`). Confidence: 0.7
- Wants projects to include a README.md documenting project details (structure, exercise/lab overview, and how to run things) — and explicitly asks for the README to be updated in the same pass when bulk-editing the practice folder ("update the reademe file as well" when commenting all ex_* labs), and repeatedly asks for the README to be refreshed whenever new practice programs are added ("update the readme file with new pratice program"), keeping docs in sync with the code. Confidence: 0.95
- Wants READMEs in subproject folders too (e.g., a test folder like `autoweave`), with proper step-by-step setup and run instructions for upcoming/recurring work in that folder. Confidence: 0.7
- Wants READMEs to also document the exact environment configuration steps performed ("how Python was configured"), including venv creation, dependency/browser installs, and IDE interpreter selection (VS Code Ctrl+Shift+P → "Python: Select Interpreter"), so the setup is reproducible and verifiable. Confidence: 0.7
- Wants the README placed at the repository root (not nested in a subfolder) so GitHub renders it on the repo's landing page for visitors. Confidence: 0.8
- Prefers environment/dependency fixes over editing source files — keeps auto-generated scripts (e.g., Playwright MCP test files) untouched and only fixes the environment (venv, package/browser installs). Confidence: 0.6
- When learning, asks conceptual "why / how does this work" questions about Python mechanics (e.g., "why do I need to pass `fun` and call `fun()` in a decorator wrapper?") and wants detailed educational explanations — concept breakdown, the underlying mechanism (e.g., `@` as desugaring to `numbers = deco1(numbers)`, closures), and key takeaways — rather than a quick fix or just corrected code. Also restates the explanation back point-by-point asking for confirmation ("... correct?", "the one which is declared will be no use right?") and follows up with edge-case "what if..." questions (e.g., "what if I don't return the wrapper?", "what if I haven't passed `self` in the constructor?") to understand failure modes and the exact contract; also asks these "why" questions about their own practice-file code (e.g., "why f1 and f2 is not calling in main function?" — wanting the top-down execution-order rule and the corrected standard pattern explained, not a file edit). Confidence: 1.0
- Wants the discussion/explanation persisted as detailed comments directly inside the learning code file (e.g., "add detailed comments of what we discussed and detailed explanation of the wrapper in this file", "add proper comments for this program", "explain what is tuple and all basic things which i need to know") so the file itself becomes the study reference, not just the chat — applies across learning files (decorator.py, lambda.py, Lab099_Tuple.py, etc.). The comments should be a self-contained concept tutorial from scratch: definition, key properties vs. related concepts (e.g., tuple vs list), common operations, and classic gotchas (e.g., the `(3,)` one-element tuple), not just line-by-line annotations of the existing code. Also requests this at whole-folder scale — "update the proper comments for all the files, which are ex_18_OOPs_Python" — i.e., expects every file in an entire learning-exercise folder (all 22 OOP files) to be commented as a study reference in one pass, with code left untouched and each file getting a topic header plus concept explanations; also requests this at whole-repo scale ("add comments on each file, where ever is required" for the README + practice files pass). Confidence: 0.97
- Wants to rewrite learning-exercise programs themselves as part of understanding — asks for proper comments specifically so they can rewrite the program from scratch ("with proper comments so that i will rewrite this program"), i.e., the commented walkthrough is a template for their own rewrite, not a final deliverable the agent writes for them. Confidence: 0.5
- When learning a concept, wants a concrete example for each part (e.g., one example per `print()` parameter: values, sep, end, file, flush) and ideally one combined example using all parts together — example-driven teaching rather than theory alone. Confidence: 0.7
- Is learning Python specifically to build agents; appreciates when explanations tie concepts to agent-building use cases (e.g., `print(..., file=f, flush=True)` framed as real-time agent logging) and frames reference-content requests around what is usable "in our automation or in AI agent building part" (asked which built-ins/os functions matter for automation/AI agents when requesting the poster). Confidence: 0.6
- Does not want files modified without explicit approval — explain first, then ask before changing anything (explicitly says "do not change" when asking for diagnosis). Confidence: 0.95
- When debugging, prefers to be told what to change so they can understand and apply the fix themselves, rather than the agent editing the file directly ("just tell me what to change, I will first understand and update"); then applies the fix and asks the agent to review ("i have modified, can you check code now"). Also makes their own code-state edits between turns (e.g., uncommenting a decorator, adding `self` to a constructor) and expects the agent to pick up the new state from the file rather than assuming the old one. Confidence: 0.86
- Uses VS Code as their IDE (references the Ctrl+Shift+P → "Python: Select Interpreter" workflow). Confidence: 0.6
- Also writes Playwright tests in TypeScript and treats their working TS code as the canonical reference — wants Python equivalents ported 1:1 from the proven TS selectors/methods (with the snake_case/await mapping explained) rather than freshly invented locators ("this code is working in typescript, tell me exactly what i need to change"). Confidence: 0.6
- Prefers keeping old code blocks as comments in the file rather than deleting them, so the old code stays as reference next to the fix. Confidence: 0.4
- When a test is skipping or failing, wants the agent to run the test itself to reproduce and get the real reason (e.g., "can you run this test and tell me why it is skipping", "run this program and give me the exact issue") rather than diagnosing from reading the code alone. Confidence: 0.6
- Runs Playwright tests in headed mode and reports visual UI observations (e.g., "it is not even clicking on the Remind later button", "the popup is not closing", "logout button is not clicking in ui, while executing") as debugging clues. Confidence: 0.6
- When asking to validate a learning-exercise program, wants a clear correct/wrong verdict plus multiple alternative solutions presented as options ("give me the other solutions as well", "let me know if any alternative solutions if required", "give me the answer with options") — e.g., a minimal fix, a cleaner/idiomatic version, and a safer/robust version — rather than just one corrected snippet. Confidence: 0.9
- Wants social-media content generated to document the learning journey — repeatedly asks for LinkedIn post drafts ("generate the linked in post content", "genearate a linked in post for todays work and practice next step we are building crew.ai framework") framing the Python study as a path to building AI agents, i.e., learning-in-public content; the post should recap today's practice AND preview the next learning step (e.g., building CrewAI agents), and gets appended to their existing `linkedin_post.md` following the established post style. Confidence: 0.8
- Manages Python tooling with uv — installed Python via uv and relies on uv-managed interpreters/venvs rather than system installers on PATH (on this Windows machine `python` is only the Store stub and `py` isn't installed; scripts are run with the uv-managed interpreter located via `uv python find`). Confidence: 0.8
- Runs Python learning labs with the VS Code Code Runner extension and expects it configured to actually work (executor pointing at a real interpreter, not the Windows Store `python` stub). Confidence: 0.6
- When something won't run, wants the agent to go through the complete project setup (VS Code settings, PATH, venv, interpreter locations) to find the root cause rather than looking only at the failing file. Confidence: 0.5
- Reports run failures by pasting the exact terminal output/error verbatim (e.g., the full `[Running] ... exited with code=9009` block, a raw PowerShell `CommandNotFoundException` for a mistyped `.venv` / unrecognized `pip`, or a full run transcript including "Login Failed" and the printed values) as the debugging clue. Confidence: 0.8
- Before deleting or cleaning up redundant config (e.g., a subfolder `.vscode/settings.json`), asks consequence-checking questions to confirm existing workflows (like Code Runner running labs in that folder) will keep working — wants assurance before removing anything. Confidence: 0.6
- Wants the agent to verify config changes/cleanups by actually running the code afterwards (e.g., after deleting a redundant `.vscode` folder, run the lab with the exact command Code Runner would use) to confirm the fix works — not just report that the change was made. Confidence: 0.5
- When a CLI/install command fails (e.g., `pip install python -dotenv`), asks "why this is not working" and wants the root cause explained — typo semantics (the space splits one package into two), broken `python`/`pip` on PATH (Windows Store stub), etc. — plus the exact corrected command (e.g., `uv pip install python-dotenv`), not just a silent fix. Confidence: 0.6
- Works on Windows with PowerShell as the default terminal, so shell guidance for Python environment how-tos (venv activation, installs) should be given in PowerShell syntax (e.g., `.\\.venv\\Scripts\\Activate.ps1`) and should present both the standard way (`python -m venv` / activate script) and the uv-native way (`uv venv` / `uv run`, which bypasses activation) since their `python` is only the Store stub — no single command, but the full runnable workflow for their setup. Confidence: 0.5
- When asking about a concept, explicitly directs the agent to read the relevant learning folder/file in the repo first and wants the explanation grounded in those actual example programs ("read the encapsulation folder basic program and explain me", "can you explain me this program, abc import? and complete program explanation please" pointing at a specific exercise file), not generic theory; also frames questions as contrasts between closely related/confusable concepts (e.g., "encapsulation is different from abstraction") to understand where the boundary is. Confidence: 0.7
- For "explain this program" requests in numbered learning-exercise sequences (e.g., `143_Abs.py`, `144_Abs.py`, `145_REAL_.py`), wants the agent to read the sibling/next files in the folder and explain the concept as a progression — how the pattern evolves across the examples — rather than explaining only the single file in isolation. Confidence: 0.7
- Delegates creation of small setup/config files to the agent when asked — e.g., after being told a `.env` with `USERNAME`/`PASSWORD` was needed, replied "create one for me" and expected the agent to write the file directly (with content matching the project's existing credential style), rather than being handed instructions to do it themselves; this repeats for each dotenv-based learning script ("for this file also, create .env file with this this folder"), i.e., expects every such script to get its own `.env` in its own folder using the established credential keys; contrasts with code debugging, where they prefer to apply fixes themselves. Confidence: 0.75
- When a `.env`-based login script keeps failing despite the env file being in place, wants the root cause diagnosed empirically (run the exact same script/command from the actual working directory and print what `os.getenv` resolves to) rather than assuming the file is the only variable — surfaced that `load_dotenv()` uses the current working directory (not the script's folder), that the venv was launched from the repo root, and that the plain name `USERNAME` collides with a pre-existing real Windows environment variable that shadows the `.env` value. The durable lesson: `.env` files should be loaded via an explicit script-relative path (`load_dotenv(Path(__file__).parent / ".env")`) and env var names should be prefixed/app-specific (e.g., `VWO_USERNAME`) to avoid collisions with real machine env vars. Confidence: 0.6
- After the agent has fully diagnosed and explained a fix (root cause + exact changes), sometimes delegates the actual edits back to the agent with a brief "do the required changes" — i.e., the "explain first, get approval before editing" rule still holds, but once the explanation is given the user doesn't always want to apply it themselves; they may explicitly hand the implementation to the agent and then expect verification by running the script. Confidence: 0.5
 handed instructions to do it themselves; contrasts with code debugging, where they prefer to apply fixes themselves. Confidence: 0.5
- The "why is this not working → explain the root cause + exact corrected line" pattern also applies to errors in their own Python learning-file code, not just CLI/install failures — e.g., a `SyntaxError` from writing `import abc from ABC, abstractmethod` prompted "why i am not able to import ABC?", and the wanted answer was the mechanism (the two forms of `import`: whole-module vs. `from ... import ...`) plus the corrected `from abc import ABC, abstractmethod`, not a silent fix. Confidence: 0.5
- When the request is purely conceptual — "read this folder and confirm my understanding" — explicitly does NOT want the agent to run/execute the code, just read the files and explain ("no need to run, just reed the ex_19_Package and tell me the understandig thats it"); running/verification is reserved for debugging failures or confirming a fix/config change. Confidence: 0.6
- Wants reference catalogs/overviews persisted as files in the numbered learning folders — this repeats as a pattern: asked to "list down all the modules available in python ... add in one file and add in 10_Modules folder", then "find all the Built in functions available in python, and list down in the same folder, with different file name", i.e., a categorized comment-only reference file (one-line description + usage example per item) placed in the relevant topic folder following its existing numbering pattern (next number in sequence, e.g., `171_Modules_Overview.py`, `172_Builtin_Functions.py`), with each catalog in its own separate file and aiming for complete coverage (all modules / all built-ins), not just common ones. Confidence: 0.8
- Wants AI-generated reusable artifacts (image prompts, reference docs, etc.) persisted into the numbered learning folder (e.g., `173_Image_Prompt.py` holding a detailed prompt as a Python string with HOW TO USE notes), explicitly "add in in this folder, so that i will use that in later" — i.e., anything reusable generated during the session gets saved as the next numbered file rather than kept only in chat. Confidence: 0.6
- When generating content about their work (LinkedIn posts, summaries), expects the agent to first read the relevant existing files — the prior `linkedin_post.md` to match the established post style, and the actual project files (e.g., the CrewAI chapter scripts) to reference accurately — rather than inventing content from memory; also keeps the new post appended to the same `linkedin_post.md` file. Confidence: 0.5
- When the agent bulk-adds study comments across the learning repo, the scope is the numbered exercise folders (ex_15 through ex_21); the user's own `Task` practice files are out of scope — the user stopped the commenting mid-pass when the agent reached the Task folder, and the agent then asked whether to revert the Task files it had already commented. Confidence: 0.5
- When a bulk edit pass is interrupted after some files were already modified, prefers to keep the completed edits rather than reverting them — after being asked whether to revert the already-commented Task practice files, replied "keep those". Confidence: 0.4
- Wants real working code from other chapters (e.g., a CrewAI agent script from chapter_12) added into the relevant topic folder (the `10_Modules` "module section") as the next-numbered study-reference copy with a study header explaining which modules/imports it uses — the original file stays untouched in its own chapter ("add this file in module section"). Confidence: 0.5
ed Task practice files, replied "keep those". Confidence: 0.4
- Wants real working code from other chapters (e.g., a CrewAI agent script from chapter_12) added into the relevant topic folder (the `10_Modules` "module section") as the next-numbered study-reference copy with a study header explaining which modules/imports it uses — the original file stays untouched in its own chapter ("add this file in module section"). Confidence: 0.5
