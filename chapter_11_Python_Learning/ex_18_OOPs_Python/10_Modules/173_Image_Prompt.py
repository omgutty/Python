# 173_Image_Prompt.py
# Topic: Image-generation prompt for a Python Built-ins & OS Module poster
#
# HOW TO USE:
#   1. Copy the `prompt` string below.
#   2. Paste it into any AI image generator (Midjourney, DALL-E,
#      Stable Diffusion, Ideogram, etc.) as the prompt.
#   3. For better text accuracy in the image, prefer Ideogram or
#      DALL-E-3 — they render text/typing more reliably.
#   4. Adjust the text content inside [SQUARE BRACKETS] before generating.

prompt = """
A stunning, ultra-detailed developer infographic poster titled
"PYTHON POWER KIT: Built-ins + OS Module for Automation & AI Agents",
rendered in a modern dark "code editor" aesthetic.

STYLE:
- Deep navy-black background (#0d1117) with a subtle grid pattern
  like VS Code.
- Vibrant glowing accents in Python brand colors: electric blue
  (#3776AB / #58A6FF) and bright yellow (#FFD43B), plus teal and
  purple gradient highlights.
- Clean, bold sans-serif headings (like Poppins or Montserrat)
  mixed with monospace code font (JetBrains Mono / Fira Code)
  for all function names.
- Soft neon glow around section cards, subtle drop shadows,
  floating translucent code-symbol shapes (braces { }, brackets
  [ ], parentheses ( ), lambda, underscores, the Python logo
  made of two intertwined snakes) in the background.
- Professional flat-design icons beside each section title:
  a gear (automation), a robot head (AI agent), a file folder,
  a terminal window, a magnifying glass, a lightning bolt.

LAYOUT (top to bottom, clean vertical grid of panels):

1. HEADER BANNER:
   Big glowing title "PYTHON POWER KIT", subtitle
   "Built-in Functions + OS Module — Your Toolkit for Automation
   & AI Agent Building". Small badge chips below: "No pip install",
   "Always available", "70+ functions".

2. SECTION: TYPE CONVERSION (icon: arrows)
   int() float() str() bool() list() tuple() set() dict()
   chr() ord() bin() hex() oct()

3. SECTION: NUMBERS & MATH (icon: calculator)
   abs() round() pow() divmod() sum() min() max()

4. SECTION: ITERATION & SEQUENCES (icon: loop arrows)
   range() len() enumerate() zip() map() filter() sorted()
   reversed() iter() next() all() any()

5. SECTION: INPUT / OUTPUT (icon: terminal)
   print() input() open() format() help() breakpoint()
   eval() exec()

6. SECTION: OBJECTS & OOP (icon: cube)
   type() id() isinstance() issubclass() getattr() setattr()
   hasattr() dir() vars() super() property()

7. SECTION: OS MODULE — AUTOMATION FAVOURITES (icon: folder/gear)
   os.getcwd()  -> current folder
   os.listdir() -> list files
   os.mkdir()   -> create folder
   os.remove()  -> delete file
   os.rename()  -> rename file
   os.path.join() -> build paths safely
   os.path.exists() -> check if file exists
   os.environ   -> read environment variables
   os.system()  -> run a shell command
   os.name      -> OS type (nt / posix)

8. SECTION: AI AGENT BUILDING — MOST USED (icon: robot)
   print(..., flush=True) -> real-time agent logging
   input()  -> ask the user for instructions
   open() / file I/O -> read & write agent data
   os.environ -> API keys & secrets
   json.dumps() / json.loads() -> talk to APIs
   type() / isinstance() -> validate agent inputs
   sorted() / filter() -> process tool results
   any() / all() -> check conditions across results

9. FOOTER:
   A highlighted callout bar: "No imports needed — built-ins are
   always ready. Pair them with os & json to automate anything."
   Small text at the very bottom: "Python Standard Library".

COLOR & LIGHTING:
- Cinematic rim lighting on the cards, high contrast, crisp vector
  edges, 8K detail, wide 16:9 composition, print-quality resolution.

RENDER QUALITY:
- "highly detailed, 8K, ultra sharp, clean vector graphics,
  professional tech poster, no clutter, perfectly aligned grid"
"""

# The [SQUARE BRACKETS] placeholders above are just guidance for you.
# Remove them before generating if you want a cleaner prompt.
