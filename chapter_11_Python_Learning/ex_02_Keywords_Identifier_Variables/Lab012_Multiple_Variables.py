# This program demonstrates assigning MANY variables in one line.
# The values match the variables in order: a=45, b=5.4, c="pramod".
a, b, c = 45, 5.4, "pramod"
# Each print() shows the value of one variable.
print(a)
print(b)
print(c)
