# This program demonstrates LITERALS - values written directly in the code.
# 20 and 65 are literals; the LAST value assigned to a variable wins.
age = 20
age = 65
# This line adds 1 but does NOT save the result, so nothing changes.
age + 1
# age is still 65, so 65 is printed.
print(age)
