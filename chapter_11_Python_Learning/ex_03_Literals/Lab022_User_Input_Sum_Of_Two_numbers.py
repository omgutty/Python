# This program takes two numbers from the user and adds them.
# input() always returns TEXT, so int() converts it into a whole number.
num1 = int(input("Enter a number: "))
num2 = int(input("Enter another number: "))
# Now both are real numbers, so + adds them and print() shows the sum.
print(num1 + num2)
