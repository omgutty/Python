# This program shows a function that takes inputs and RETURNS a result.
#Type 3 - with param and returns also.

# The function adds a and b, then sends the answer back with "return".
def sum_of_two(a, b):
    return a + b


# The returned value (9) is saved into the variable "result".
result = sum_of_two(4, 5)
print(result)